{
  "name": "open-resolve-list",
  "private": true,
  "description": "Local build and validation commands for Open Resolve List",
  "scripts": {
    "build": "node scripts/rebuild.mjs",
    "serve": "node scripts/serve-site.mjs",
    "smoke": "node scripts/site-smoke.mjs",
    "browser:smoke": "node scripts/browser-smoke.mjs",
    "maintenance:summary": "node scripts/maintenance-summary.mjs",
    "validate": "node scripts/validate-resources.mjs && node --test --test-concurrency=1 scripts/*.test.mjs",
    "review:kinds": "node scripts/review-kinds.mjs --kind other",
    "review:kinds:report": "node scripts/review-kinds.mjs --kind other --write-report"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
